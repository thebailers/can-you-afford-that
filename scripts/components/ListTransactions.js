import React from 'react';

const ListTransactions = React.createClass({
  render() {
    return (
      <ul>
        <li>Transactions here</li>
      </ul>
    );
  }
});

export default ListTransactions;
